on: push
name: Auto Deploy
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2.1.0
      with:
        fetch-depth: 2
    - name: FTP-Deploy-Action
      uses: SamKirkland/FTP-Deploy-Action@3.1.1
      with:
        ftp-server: ftp://ventadour.fr
        ftp-username: u996831204
        ftp-password: ${{ secrets.ftp_password }}
        server-dir:  public_html/dev/wp-content/themes/rsw-pestrin/
